# Store Intelligence API — Python dependencies
# Tested on Python 3.10+ (Python 3.14 compatible)

# Core API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0,<3.0.0      # v2 required — models.py uses field_validator, model_validator

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0

# Dashboard
rich>=13.0.0

# Data processing
numpy>=1.26.0
pandas>=2.0.0

# Detection pipeline (real CCTV processing — installed and working)
ultralytics>=8.4.0          # YOLOv8 + ByteTrack
opencv-python-headless>=4.9.0  # Video frame reading
lap>=0.5.12                 # ByteTrack Hungarian matching (auto-installed by ultralytics)
# torch>=2.0.0              # uncomment for GPU acceleration
# torchvision>=0.18.0       # uncomment for OSNet Re-ID
